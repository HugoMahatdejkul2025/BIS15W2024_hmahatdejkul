---
title: "RMarkdown Practice"
author: "Hugo"
date: "2024-01-11"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## Title 
### Title
#### Title

[BIS 15: Webpage](url)
```{r}
library("tidyverse")


```
#title
```{r}
z<-4+4*2
x<-2*2
f<-mean(z+x) #this will not work because it doesn't treat it as two values so it iwll just diplay the addition and assign it to the variable f
Y=z+x
mean(Y)
```

```{r}
ggplot(mtcars, aes(x = factor(cyl))) +
    geom_bar()
````

